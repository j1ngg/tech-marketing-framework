---
name: email
description: Use when you want an email sequence, especially event follow-up emails with timing guidance and ready-to-send copy.
---

# Email

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/email/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/email/SKILL.md` as the source of truth for the workflow
