---
name: ads-auditor
description: Use when you want campaign performance reviewed against benchmarks, with a health score and prioritized optimization recommendations.
---

# Ads Auditor

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/agents/ads-auditor.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md` when you produce reusable summaries
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/agents/ads-auditor.md` as the source of truth for the workflow
