---
name: blog
description: Use when you want an SEO and AEO optimized blog post from source material, including feature announcements, launches, tutorials, or thought leadership.
---

# Blog

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/blog/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/blog/SKILL.md` as the source of truth for the workflow
