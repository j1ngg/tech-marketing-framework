---
name: ads
description: Use when you want paid ad copy for Google, Meta, or LinkedIn, including headlines, descriptions, and image briefs.
---

# Ads

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/ads/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/ads/SKILL.md` as the source of truth for the workflow
