---
name: launch-roundup
description: Use when you want a weekly feature announcement package from recent merge requests, including Slack, blog, LinkedIn, and carousel outputs.
---

# Launch Roundup

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/launch-roundup/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/launch-roundup/SKILL.md` as the source of truth for the workflow
