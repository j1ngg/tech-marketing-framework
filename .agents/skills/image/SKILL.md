---
name: image
description: Use when you want marketing visuals generated from brand guidelines for posts, blog headers, ads, or decks.
---

# Image

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/image/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/image/SKILL.md` as the source of truth for the workflow
