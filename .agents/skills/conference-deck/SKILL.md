---
name: conference-deck
description: Use when you have an accepted conference abstract and want a solo-presenter deck that weaves one to three customer case studies into the promised narrative arc. Outputs Markdown outline, PPTX, and optional opener/close scripts.
---

# Conference Deck

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/conference-deck/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/conference-deck/SKILL.md` as the source of truth for the workflow
