---
name: editorial-calendar
description: Use when you want a monthly editorial planning workshop that produces a rolling calendar, themes, and a repurposing matrix.
---

# Editorial Calendar

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/editorial-calendar/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/editorial-calendar/SKILL.md` as the source of truth for the workflow
