---
name: messaging-positioning
description: Use when you want a structured messaging and positioning workshop that captures discovery answers and builds the messaging document.
---

# Messaging Positioning

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/messaging-positioning/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/messaging-positioning/SKILL.md` as the source of truth for the workflow
