---
name: autoresearch
description: Use when you want to benchmark and improve an existing repo skill through repeated evals, prompt mutations, and score tracking.
---

# Autoresearch

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/autoresearch/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/autoresearch/SKILL.md` as the source of truth for the workflow
