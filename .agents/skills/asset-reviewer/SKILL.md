---
name: asset-reviewer
description: Use when you want a strict review of a marketing asset against style rules, factual accuracy, and messaging alignment.
---

# Asset Reviewer

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/agents/asset-reviewer.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/agents/asset-reviewer.md` as the source of truth for the workflow
