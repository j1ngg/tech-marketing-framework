---
name: skill-builder
description: Use when you want to design a new repo skill or agent and need help choosing the right pattern, structure, and rules.
---

# Skill Builder

This is a Codex compatibility wrapper.

Follow the canonical workflow in:

- `../../../.claude/skills/skill-builder/SKILL.md`

Before starting:

1. Read `../../../AGENTS.md`
2. Apply the shared rules in `../../../.claude/rules/content-guidelines.md`
3. Prefer `docs/inputs-local/` over `docs/inputs/` when the local directory exists

Compatibility rule:

- Keep `../../../.claude/skills/skill-builder/SKILL.md` as the source of truth for the workflow
